## Ideas
- We can use the idea of using indices to access the elements in the array so that we don't have to swap potentially large elements.
- Use std::move for storing elements in the keys array. Be wary of old key and new key in the change function.